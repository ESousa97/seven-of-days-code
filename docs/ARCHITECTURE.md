# Architecture

The application uses Next.js App Router with three interactive React modules rendered on the home page. Component logic lives in components/projects and is composed in src/app/page.tsx.

See README for details and diagrams.
